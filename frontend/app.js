// Browser artifact for the dependency-free vertical slice. Source of truth: src/main.ts.
const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt = (value) => typeof value === 'number' ? value.toFixed(value % 1 ? 1 : 0) : esc(value);
const app = document.querySelector('#app');
function planet(s) { const p=s.system.planet; const markers=Object.entries(s.districts).map(([id,level])=>`<span class="marker ${id}" title="${esc(id)}">${level}</span>`).join(''); return `<section class="planet-stage"><div class="star-orb"></div><div class="planet-orb">${markers}</div><div class="orbit orbit-one"></div><div class="orbit orbit-two"></div><p>${esc(s.system.name)} / ${esc(p.name)}</p></section>`; }
function render(s) { const energy=s.capacities.energy_generation-s.capacities.energy_consumption; app.innerHTML=`<header><div class="brand">STAR<span>Z</span></div><div class="hud"><b>MATÉRIA</b> ${fmt(s.stocks.raw_ore)} <b>LIGA</b> ${fmt(s.stocks.refined_alloy)} <b>COMP</b> ${fmt(s.stocks.components)} <b>ENERGIA</b> ${fmt(energy)} / ${fmt(s.capacities.energy_generation)} <b>POP</b> ${fmt(s.population.total)}</div></header><main><nav><button>Overview</button><button>Planet</button><button>Research</button><button>Shipyard</button><button>Fleets</button><button>Galaxy</button></nav><article><div class="eyebrow">OBSERVATÓRIO // SISTEMA INICIAL</div><h1>${esc(s.system.name)}</h1><div class="grid">${planet(s)}<section class="panel facts"><h2>Planeta observado</h2><div class="facts-grid"><span>Classe estelar <b>${esc(s.system.star.stellar_class)}</b></span><span>Luminosidade <b>${fmt(s.system.star.luminosity)}x</b></span><span>Gravidade <b>${fmt(s.system.planet.gravity)}g</b></span><span>Temperatura <b>${fmt(s.system.planet.temperature)} K</b></span><span>Água <b>${fmt(s.system.planet.water)}%</b></span><span>Campo magnético <b>${fmt(s.system.planet.magnetic_field)}</b></span></div></section></div><div class="columns"><section class="panel"><h2>Desenvolvimento</h2><div class="rows">${Object.entries(s.districts).map(([id,level])=>`<div><span>${esc(id.replaceAll('_',' '))}</span><b>nível ${level}</b></div>`).join('')}</div><button class="primary" data-action="build" data-id="processor">Construir processador</button></section><section class="panel"><h2>Pesquisa</h2><p>${s.research.active?`Em andamento: <b>${esc(s.research.active)}</b>`:'Nenhuma pesquisa em andamento.'}</p><button class="primary" data-action="research" data-id="orbital_engineering">Iniciar engenharia orbital</button></section><section class="panel"><h2>Frotas</h2>${s.fleets.length?s.fleets.map(f=>`<div class="fleet"><b>${esc(f.name)}</b><span>${esc(f.status)} → ${esc(f.destination)}</span></div>`).join(''):'<p class="muted">Nenhuma frota em trânsito.</p>'}<button class="primary" data-action="build-ship">Montar scout</button><button data-action="travel" data-mode="NORMAL">Enviar em normal</button></section></div><section class="panel route"><h2>Galaxy / rota local</h2><p class="muted">Selecione um sistema próximo para comparar a mesma frota em três regimes.</p><div class="system-choice"><button class="selected">Asterion +1:0 <small>vizinho</small></button><button disabled>Asterion -1:0 <small>não explorado</small></button></div><div class="mode-row"><button data-action="preview" data-mode="ECONOMY">Economy</button><button data-action="preview" data-mode="NORMAL">Normal</button><button data-action="preview" data-mode="FORCED">Forced</button></div><div id="route-preview" class="route-preview">Selecione um regime para ver ETA, combustível, calor e assinatura.</div></section><div class="notices">${s.notices.map(n=>`<span>${esc(n)}</span>`).join('')}</div></article></main>`; app.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>act(b.dataset.action,b.dataset.id,b.dataset.mode)); }
async function act(action,id,mode='NORMAL') { const body=action==='build'||action==='research'?{id}:action==='travel'||action==='preview'?{target_x:1,target_y:0,propulsion_id:'chemical_drive',mode}:{}; const response=await fetch(`/api/${action==='preview'?'travel-preview':action}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}); if(action==='preview'){const previews=await response.json(); document.querySelector('#route-preview').innerHTML=Object.entries(previews).map(([name,value])=>`<span><b>${name}</b> ETA ${Math.round(value.eta_seconds/60)}m · fuel ${value.fuel_cost} · heat ${value.heat}</span>`).join(''); return;} load(); }
async function load() { render(await (await fetch('/api/state')).json()); } load();
